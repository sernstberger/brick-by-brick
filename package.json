{
  "name": "brick-by-brick",
  "version": "0.1.0",
  "description": "Interactive 3D step-by-step assembly viewer for LDraw brick models",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "verify": "node scripts/verify.mjs all",
    "library:index": "node scripts/index-library.mjs"
  },
  "dependencies": {
    "sirv": "^3.0.2",
    "three": "0.186.0",
    "vite": "8.2.2"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
